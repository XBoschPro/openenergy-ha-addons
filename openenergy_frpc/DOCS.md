## OpenEnergy FRP Client

Base add-on that runs `frpc` with a generated `frpc.toml`.

Next step: replace manual fields with an OpenEnergy API bootstrap (Keycloak login will live in the integration).

